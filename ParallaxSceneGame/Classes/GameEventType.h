#ifndef GAME_EVENT_TYPE_H
#define GAME_EVENT_TYPE_H

// Define an enum class for different game event types
enum class GameEventType {
    JumpAction,
    // Add other game event types as needed
};

#endif // GAME_EVENT_TYPE_H